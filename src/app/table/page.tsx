import Table from "@/components/Table/Table";
import ReactTable from "@/components/Table/ReactTable";

const page = () => {
  return (
    <div>
      <Table />
      <ReactTable />
    </div>
  );
};

export default page;
